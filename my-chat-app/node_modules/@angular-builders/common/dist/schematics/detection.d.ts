import { Tree } from '@angular-devkit/schematics';
import { workspaces } from '@angular-devkit/core';
export type TestBuilderKind = 'karma' | 'jest' | 'vitest' | 'other' | 'none';
export declare function getProjectsToTarget(workspace: workspaces.WorkspaceDefinition, optionProject?: string): string[];
export declare function detectTestBuilder(workspace: workspaces.WorkspaceDefinition, projectName: string): TestBuilderKind;
export declare function isZoneless(tree: Tree, workspace: workspaces.WorkspaceDefinition, projectName: string): boolean;
